# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage
/playwright-report
/test-results

# env files (NEVER commit secrets)
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vercel
.vercel

# editor
.idea
.vscode/*
!.vscode/extensions.json

# local claude settings (machine-specific)
.claude/settings.local.json

test-artifacts/

# local-only secrets (never commit)
CREDENTIALS.md

# reference material from another company — local inspiration only, NEVER commit
# (contains their proprietary workflow + a production DB schema dump)
old-projects-context/
temp/